namespace _521Final.Models
{
    public class Search
    {
        public int Id { get; set; }

       /* private List<string> items;

        public Search(List<string> items)
        {
            this.items = items;
        }

        public List<string> SearchItems(string searchTerm)
        {
            return items.Where(item => item.Contains(searchTerm)).ToList();
        }*/
    }
}
